#!/bin/sh
#set -x
NARGS=$#
N_1=$((NARGS-1))
export N_1
CMD=$(echo "$@" | cut -d" " -f 1-${N_1})
EXE=$(echo "$@" | cut -d" " -f "${NARGS}")
mpirun ${CMD} $(which "${EXE}")
#echo CMD=$CMD
#echo EXE=$EXE
