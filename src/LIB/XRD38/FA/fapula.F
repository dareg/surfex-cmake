C Jan-2011 P. Marguinaud Interface to thread-safe FA
      SUBROUTINE FAPULA(KREP, KRANG, PSPEC, KPULAP )
      USE FA_MOD, ONLY : FA_COM_DEFAULT,
     S                   FA_COM_DEFAULT_INIT,
     S                   NEW_FA_DEFAULT
      IMPLICIT NONE
#include "precision.h"
      INTEGER KREP, KRANG, KPULAP
      REAL (KIND=JPDBLR) PSPEC(*)
      IF (.NOT. FA_COM_DEFAULT_INIT) CALL NEW_FA_DEFAULT ()
      CALL FAPULA_MT (FA_COM_DEFAULT, KREP, KRANG, PSPEC, KPULAP )
      END SUBROUTINE

