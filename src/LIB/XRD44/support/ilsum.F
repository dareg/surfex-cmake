      FUNCTION ILSUM(N,LX,INCX)
      USE PARKIND1, ONLY : JPIM
      IMPLICIT NONE
      INTEGER(KIND=JPIM) :: ILSUM
      INTEGER(KIND=JPIM) :: N
      INTEGER(KIND=JPIM) :: INCX
      INTEGER(KIND=JPIM) :: JL
      LOGICAL :: LX(*)
!
!  FINDS THE NUMBER OF TRUE ELEMENTS OF A LOGICAL VECTOR
!
      ILSUM=0
      IF ( N.LE.0) RETURN
!
      DO 1 JL=1,N
        IF ( LX(1+(JL-1)*INCX) ) ILSUM=ILSUM+1
 1    CONTINUE
!
      ENDFUNCTION ILSUM
